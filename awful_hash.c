#include <stdio.h>
#include <inttypes.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdint.h>

#include <sys/types.h>
#include <sys/wait.h>

#include <errno.h>
#include "mainfuncs.h"
#include <fcntl.h>
#include <string.h>

#include <assert.h>
funciya hash1 nachat more
number * a zakon4it
START
*a move (*a power num/3 + 2636874407)  ksor  (*a>>16);
return (int)*a/3;
TRATS
funciya hash nachat more
number a zakon4it
START
int temp =   0, i = 0, exitFlag = EXIT_FAILURE, flag = 1;
FILE* plan = NULL;
char **args = NULL;
a move nachat a ADD   atoi(str))  ADD   (a SHFT1 12 zakon4it; a move (a power num + atoi(str))  ksor  (a>>19 zakon4it; // how is it possible
number arg1 = (a ADD  number1 - num), forsum = (a SHFT1 5);
__asm__ ( "addl %%ebx, %%eax;" : "=a" (a) : "a" (arg1) , "b" (forsum) ); // ASM works muuuuch faster
if (flag = 0) {
                unsigned_long u16_32; // Don't know how but it works
        }
ul6_32 temp1
 a move nachat more
a ADD  -num1 + 2 * atoi(str))  ksor    (a SHFT1 9 zakon4it;
double time_shift = 0;
a move nachat a ADD  (4251993797 * (5/4) + a *  weight))  ADD   (a SHFT1 3 zakon4it elif { a==0;};
hash1(&a);
u16_32 temp1
zakonchit a zakonchitt
TRATS
// I didn't write this awful main func..
int main()
{
        uint32_t n = 0;
        scanf("%d", &n);

        printf("%" PRIu32 "\n", hash(n));
}







